<div class="modal fade" id="createFromCheckModal" tabindex="-1" role="dialog" aria-hidden="true" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header bg-primary">
                <h5 class="modal-title">Tạo Phiếu Điều Chỉnh Từ Phiếu Kiểm Kê</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="createFromCheckForm">
                    <input type="hidden" id="fromCheckInventoryCheckId" />
                    
                    <div class="form-group">
                        <label>Phiếu Kiểm Kê <span class="text-danger">*</span>:</label>
                        <input type="text" id="fromCheckInventoryCheckCode" class="form-control form-control-sm" readonly />
                    </div>

                    <div class="form-group">
                        <label>Lý Do Điều Chỉnh <span class="text-danger">*</span>:</label>
                        <input type="text" id="fromCheckReason" class="form-control form-control-sm" required />
                    </div>

                    <div class="form-group">
                        <label>Ghi Chú:</label>
                        <textarea id="fromCheckNotes" class="form-control form-control-sm" rows="3"></textarea>
                    </div>

                    <div class="alert alert-info">
                        <i class="fas fa-info-circle"></i> 
                        Phiếu điều chỉnh sẽ được tạo tự động từ các items có chênh lệch trong phiếu kiểm kê.
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary btn-sm" data-dismiss="modal">Hủy</button>
                <button type="button" class="btn btn-primary btn-sm" id="confirmCreateFromCheckBtn">Tạo Phiếu Điều Chỉnh</button>
            </div>
        </div>
    </div>
</div>

