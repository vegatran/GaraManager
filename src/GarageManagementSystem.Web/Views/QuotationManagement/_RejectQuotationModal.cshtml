<!-- Modal Từ Chối Báo Giá -->
<div class="modal fade" id="rejectQuotationModal" tabindex="-1" role="dialog" aria-labelledby="rejectQuotationModalLabel" aria-hidden="true" data-backdrop="static" data-keyboard="false">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header bg-danger">
                <h5 class="modal-title" id="rejectQuotationModalLabel">
                    <i class="fas fa-times-circle"></i> Từ Chối Báo Giá
                </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="rejectQuotationForm">
                    <!-- Lý do từ chối -->
                    <div class="form-group">
                        <label for="rejectReason">
                            Lý do từ chối <span class="text-danger">*</span>
                        </label>
                        <textarea class="form-control form-control-sm" id="rejectReason" rows="4" 
                                  placeholder="Nhập lý do từ chối báo giá..." required></textarea>
                        <small class="form-text text-muted">Vui lòng nhập lý do cụ thể để từ chối báo giá này.</small>
                    </div>
                    
                    <!-- Tùy chọn tính phí kiểm tra -->
                    <div class="form-group">
                        <div class="custom-control custom-checkbox">
                            <input type="checkbox" class="custom-control-input" id="rejectChargeInspectionFee">
                            <label class="custom-control-label" for="rejectChargeInspectionFee">
                                <strong>Tính phí kiểm tra</strong>
                            </label>
                        </div>
                        <small class="form-text text-muted">
                            Nếu được chọn, hệ thống sẽ tính phí kiểm tra xe cho khách hàng khi từ chối báo giá.
                        </small>
                    </div>
                    
                    <!-- Thông báo -->
                    <div class="alert alert-warning">
                        <i class="fas fa-exclamation-triangle"></i>
                        <strong>Lưu ý:</strong> Sau khi từ chối, báo giá sẽ chuyển sang trạng thái "Đã từ chối" và không thể chỉnh sửa. 
                        Nếu cần tính phí kiểm tra, vui lòng tích vào ô phía trên.
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary btn-sm" data-dismiss="modal">
                    <i class="fas fa-times"></i> Hủy
                </button>
                <button type="button" class="btn btn-danger btn-sm" id="btnSubmitRejectQuotation">
                    <i class="fas fa-ban"></i> Xác nhận Từ chối
                </button>
            </div>
        </div>
    </div>
</div>

<!-- JS moved to quotation-management.js -->

